# House Duck 공개 사이트 보안 정책

## 공개 범위

이 저장소에는 웹 방문자에게 공개되어도 되는 정적 HTML, 이미지, 법률 문서,
`app-ads.txt`, 검색엔진 인증 정보만 둡니다. 사이트가 공개되는 한 이러한
파일은 저장소 공개 여부와 관계없이 방문자에게 전달됩니다.

다음 항목은 절대 커밋하지 않습니다.

- 게임 프로젝트와 빌드 파일
- Supabase 서비스 역할 키, API 비밀키, 비밀번호, 액세스 토큰
- Android 서명 키와 인증서 원본
- 사용자 UID 목록, 이메일 목록, 로그, 데이터베이스 덤프
- `.env` 및 로컬 도구 설정

## 운영 원칙

- GitHub와 도메인 등록기관 계정에 2단계 인증을 사용합니다.
- `houseduck.in`은 GitHub에서 소유권 검증하고 HTTPS를 강제합니다.
- 공개 저장소에 민감 정보가 커밋되면 삭제만 하지 않고 해당 키를 즉시 폐기하고 재발급합니다.
- 정적 콘솔에는 공개되어도 되는 UI와 publishable key, OAuth client ID만 둡니다. 허용 계정, 2차 질문 검증, 관리자 세션 서명, `service_role` 쓰기는 Supabase 서버가 처리합니다.
- 콘솔은 원본 게임 테이블을 직접 쓰지 않습니다. 관리자 Edge Function의 고정 action allowlist와 서버 전용 감사 RPC만 사용하며, 실수정은 기본 비활성 플래그를 별도 확인합니다.
- Gmail 액세스 토큰은 CS 화면의 브라우저 메모리에만 두고 로그·URL·쿠키·Web Storage·Supabase에 저장하지 않습니다. 메일 본문은 HTML로 실행하지 않고 일반 텍스트로만 표시합니다.
- challenge 정답·해시, 관리자 세션 비밀값, 메일·첨부·영수증·사용자 데이터는 이 저장소에 넣지 않습니다.

## 보안 문제 제보

공개 이슈에 취약점이나 개인정보를 작성하지 말고 `support@houseduck.in`으로
알려주세요.
